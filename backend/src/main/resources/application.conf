ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [com.yoco.cashregister.ApplicationKt.module]
    }
}

database {
    url = "jdbc:postgresql://localhost:5432/cashregister"
    url = ${?DATABASE_URL}
    user = "cashregister"
    user = ${?DATABASE_USER}
    password = "cashregister"
    password = ${?DATABASE_PASSWORD}
}
